mobilefacenet.tflite is a placeholder file. Replace it with a valid TFLite model file.

To run the test:

1. Install dependencies: `pip install -r requirements.txt`
2. From repo root: `python test_model.py`

Expected: the test script prints input/output shapes and an embedding vector.
